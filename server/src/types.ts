export interface Settings {
  dataDir: string
  scanRoots: string[]
  archiveRoots: string[]
  excludePaths: string[]
  llmEnabled: boolean
  llmApiKey: string
  llmBaseUrl: string
  llmModel: string
}
